import type { CensusRecord } from "@/domain/census";

export function TerritoryKpis({accessCount,records,streetCount}:{accessCount:number;records:CensusRecord[];streetCount?:number}){const news=records.filter(r=>r.contactType==="Notizia").length,appraised=records.filter(r=>r.isAppraised).length,external=records.filter(r=>r.engagementType==="Incarico altre agenzie").length,exclusive=records.filter(r=>r.engagementType==="In esclusiva").length;const items=[...(streetCount===undefined?[]:[["Vie / indirizzi",streetCount] as const]),["Civici",accessCount] as const,["Contatti",records.length] as const,["Notizie",news] as const,["In valutazione",appraised] as const,["Altre agenzie",external] as const,["Esclusive",exclusive] as const];return <section className="territory-kpis" aria-label="Indicatori censimento">{items.map(([label,value])=><article key={label}><strong>{value}</strong><span>{label}</span></article>)}</section>}
