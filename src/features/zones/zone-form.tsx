"use client";
import Link from "next/link";
import { useState } from "react";
import { Check, Save } from "lucide-react";
import { operators, streets } from "@/lib/demo-data";
export function ZoneForm(){const [saved,setSaved]=useState(false);return <form className="record-form" onSubmit={e=>{e.preventDefault();setSaved(true)}}>{saved&&<div className="success-banner"><Check/> Zona validata nella demo. Scegli una via per continuare.</div>}<section className="form-section"><div className="section-title"><span>01</span><div><h2>Territorio e assegnazione</h2><p>Gerarchia territoriale esplicita</p></div></div><div className="fields-grid"><label>Nazione *<input required defaultValue="Italia"/></label><label>Regione *<input required defaultValue="Emilia-Romagna"/></label><label>Provincia *<input required defaultValue="Bologna"/></label><label>Comune *<input required defaultValue="Bologna"/></label><label>Nome zona *<input required placeholder="es. Murri"/></label><label>Assegnatario *<select required defaultValue=""><option value="">Seleziona…</option>{operators.map(o=><option value={o.id} key={o.id}>{o.name}</option>)}</select></label></div></section><section className="form-section"><div className="section-title"><span>02</span><div><h2>Definisci le vie</h2><p>La relazione resta esplicita e modificabile</p></div></div><div className="check-grid">{streets.map(s=><label className="check-card" key={s.id}><input type="checkbox"/><span>{s.name}<small>{s.municipality}</small></span></label>)}</div></section><div className="form-footer"><Link className="button secondary" href="/censimento/zone">Salva e torna all’elenco</Link><button className="button primary"><Save size={17}/> Salva e definisci le vie</button></div></form>}
