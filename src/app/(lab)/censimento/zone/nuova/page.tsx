import { PageHeader } from "@/components/ui";import { ZoneForm } from "@/features/zones/zone-form";import { loadCensusData } from "@/services/census-data";import { hasSupabaseEnvironment } from "@/lib/supabase/server";export default async function NewZone(){const {countries,regions,provinces,municipalities,operators,streets}=await loadCensusData();return <><PageHeader eyebrow="Censimento / Zone" title="Nuova zona" description="Definisci territorio, assegnatario e collegamenti alle vie."/><ZoneForm {...{countries,regions,provinces,municipalities,operators,streets}} databaseMode={hasSupabaseEnvironment()}/></>}
