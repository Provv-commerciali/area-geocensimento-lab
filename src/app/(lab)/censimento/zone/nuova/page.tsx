import { PageHeader } from "@/components/ui";import { ZoneForm } from "@/features/zones/zone-form";import { loadCensusData } from "@/services/census-data";import { hasSupabaseEnvironment } from "@/lib/supabase/server";export default async function NewZone(){const {countries,regions,provinces,operators}=await loadCensusData(["countries","regions","provinces","operators"]);return <><PageHeader eyebrow="Censimento / Zone" title="Nuova zona" description="Definisci territorio, assegnatario e vie o indirizzi da censire."/><ZoneForm {...{countries,regions,provinces,operators}} databaseMode={hasSupabaseEnvironment()}/></>}
