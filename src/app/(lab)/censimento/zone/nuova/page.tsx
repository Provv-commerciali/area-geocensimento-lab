import { PageHeader } from "@/components/ui";import { ZoneForm } from "@/features/zones/zone-form";import { loadCensusData } from "@/services/census-data";export default async function NewZone(){const {operators,streets}=await loadCensusData();return <><PageHeader eyebrow="Censimento / Zone" title="Nuova zona" description="Definisci territorio, assegnatario e collegamenti alle vie."/><ZoneForm {...{operators,streets}}/></>}
