import { notFound } from "next/navigation";
import { PageHeader } from "@/components/ui";
import { latestInterview } from "@/domain/census";
import { loadCensusData } from "@/services/census-data";
export default async function RecordPage({params}:{params:Promise<{recordId:string}>}){const [{recordId},{records}]=await Promise.all([params,loadCensusData()]);const r=records.find(x=>x.id===recordId);if(!r)notFound();const latest=latestInterview(r);return <><PageHeader eyebrow="Scheda censimento" title={`${r.lastName} ${r.firstName??""}`} description={`${r.zoneName} · ${r.streetName}, ${r.civicNumber}${r.civicExtension?`/${r.civicExtension}`:""}`}/><div className="detail-grid"><section className="panel"><h2>Anagrafica</h2><dl><div><dt>Tipologia</dt><dd><span className="badge news">{r.contactType}</span></dd></div><div><dt>Telefono</dt><dd>{r.phone??"—"}</dd></div><div><dt>Responsabile</dt><dd>{r.responsibleOperatorName??"—"}</dd></div><div><dt>Ereditato</dt><dd>{r.inherited?"Sì":"No"}</dd></div><div><dt>Perizia immobiliare</dt><dd>{r.isAppraised?"Sì, flag manuale":"No"}</dd></div></dl></section><section className="panel"><h2>Immobile e catasto</h2><dl><div><dt>Porzione</dt><dd>{r.buildingScope}</dd></div><div><dt>Piano</dt><dd>{r.floorLabel??"—"}</dd></div><div><dt>Superficie</dt><dd>{r.surface?`${r.surface} m²`:"—"}</dd></div><div><dt>Complesso</dt><dd>{r.complexName??"—"}</dd></div><div><dt>Foglio / particella / sub.</dt><dd>{[r.sheet,r.parcel,r.subaltern].filter(Boolean).join(" / ")||"—"}</dd></div></dl></section></div><section className="panel history"><div className="panel-heading"><div><p className="eyebrow">Storico reale</p><h2>Interviste</h2></div><span>{r.interviews.length} eventi</span></div>{r.interviews.length?<div className="timeline">{[...r.interviews].sort((a,b)=>b.interviewDate.localeCompare(a.interviewDate)).map(i=><article key={i.id}><span className="timeline-dot"/><time>{i.interviewDate}</time><div><strong>{i.outcome??"Intervista"}</strong><p>{i.response??"Nessuna risposta registrata"} · {i.reason??"Nessun motivo"}</p><small>{i.operatorName}{i.recallDate?` · Ricontatto ${i.recallDate}`:""}</small></div></article>)}</div>:<p className="muted">Nessuna intervista registrata.</p>}{latest&&<small className="muted">Ultima intervista derivata: {latest.interviewDate}</small>}</section></>}
