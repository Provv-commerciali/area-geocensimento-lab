import { PageHeader } from "@/components/ui";
import { ContactForm } from "@/features/census/contact-form";
import { loadCensusData } from "@/services/census-data";
import { hasSupabaseEnvironment } from "@/lib/supabase/server";
import { getCadastralContactPrefill, getCadastralUnitPrefill } from "@/services/cadastral-data-service";
export default async function NewContactPage({searchParams}:{searchParams:Promise<{cadastralRightId?:string;cadastralUnitId?:string;zoneId?:string;streetId?:string;civicId?:string}>}){const params=await searchParams;const [{zones,streets,civics,complexes,operators},loaded]=await Promise.all([loadCensusData(["zones","streets","civics","complexes","operators"]),params.cadastralRightId?getCadastralContactPrefill(params.cadastralRightId):params.cadastralUnitId?getCadastralUnitPrefill(params.cadastralUnitId):Promise.resolve(undefined)]);const prefill=loaded??(params.zoneId&&params.streetId?{zoneId:params.zoneId,streetId:params.streetId,civicId:params.civicId,subjectType:"PRIVATO" as const,sheet:"",parcel:""}:undefined);return <><PageHeader eyebrow="Censimento / Contatti" title="Nuovo contatto" description={prefill?"Il contesto territoriale è già selezionato. Completa soltanto i dati del contatto.":"Registra un privato o un’azienda nel proprio contesto immobiliare. Le interviste si aggiungono solo con un’azione successiva esplicita."}/><ContactForm {...{zones,streets,civics,complexes,operators,prefill}} databaseMode={hasSupabaseEnvironment()}/></>}
