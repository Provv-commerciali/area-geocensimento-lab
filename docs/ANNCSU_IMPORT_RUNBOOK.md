# ANNCSU Toscana source — Lucca LAB import

Scope: isolated `area-geocensimento-lab` only. Never use production or ProvviOne credentials. The three `202609240002`–`004` migrations are a breaking demo-data reset. Apply them only after verifying the target project and making any desired LAB backup. Run the ISTAT territorial sync first. The LAB import is limited to Provincia ISTAT `046` (Lucca): all 33 municipalities must exist with matching ISTAT and cadastral codes. The source remains the original regional ZIP pair.

1. Validate the Lucca subset without a database: `npm run anncsu:sync -- --province 046`. This reads both original ZIPs, filters on the three-digit ISTAT province code, verifies headers, UTF-8, selected IDs, municipality pairing, Street/Access relations, numbering, access totals, coordinate methods and territorial envelope. It prints source ZIP SHA-256 and selected anomalies.
2. Set `ANNCSU_DATABASE_URL` only in the local process environment, using the dedicated LAB direct PostgreSQL connection. Do not commit it or put it in `NEXT_PUBLIC_*`.
3. After applying the versioned migrations, run `npm run anncsu:sync -- --province 046 --apply`. The importer requires an explicit province for LAB writes, validates the selected pair before database writes, gates the municipality joins, stages rows in one transaction, uses official progressive keys for upsert, records revisions/observations and marks missing official rows in that province not current without deleting operational FKs. The same SHA-256 pair and scope are a no-op. A changed hash for the same release date/scope fails closed. Audit scope is `PROVINCE:046`, with SHA-256 of the original full ZIPs.
4. Execute `tests/database/anncsu-lucca-live-verification.sql` on the same dedicated LAB target for PostGIS numeric/round-trip and source-count controls. Verify `anncsu_import_runs`, issue rows, quarantined points, zero-access Streets, repeated import no-op, Zone↔Street municipality guards, and access visibility without records. Exercise authenticated RLS and review proposed MANUAL exceptions. Do not claim database validation from the offline dry run alone. The regional `anncsu-live-verification.sql` expects all Toscana and is **not** the Lucca acceptance test.

No source-artifact deletion job exists. Audit, SHA-256, first/last/current state and structured revisions are permanent. Original ZIP retention is configurable through `ANNCSU_SOURCE_RETENTION_MONTHS` / `artifact_retention_months` (default 12), but cleanup requires a separately designed job.

The current batch importer prioritizes integrity and transactionality over throughput; staging ~1.9 million rows and maintaining revisions/observations requires a dedicated maintenance window, disk-space monitoring and a full LAB dry run before any production-shaped deployment. Browser endpoints are bounded and never return the full catalog.

## LAB execution checkpoint — 2026-09-24

The configured project ref `fomluksjubzimkfnzouf` was verified as the dedicated LAB: 7,894 ISTAT municipalities, 273 in Regione ISTAT `09` (Toscana), PostGIS 3.3.7, and the schema objects/RLS/functions/triggers from migrations `202609240002`–`004` already present. There is no `supabase_migrations.schema_migrations` history because these migrations were applied outside the CLI; do **not** re-run the destructive `002` merely to create migration history.

The 2026-09-15 Toscana ZIP pair passed offline validation: 87,147 Streets, 1,901,458 AddressAccesses, 1,555,651 with coordinates, 345,807 without, 10,965 zero-access Streets and four quarantined points. SHA-256: Stradario `2c4978b2636f44b16efb400eab16f6b189a94ba78c1e9da77d1016744489dccd`; Indirizzario `8c755d866901adf3a53b4395595f8a9c9bbf4ffaea8077be57b8fbee16337291`.

The real import **failed** during bulk staging after approximately 22 minutes: the connection terminated, PostgreSQL briefly returned `57P03`, and database size had reached approximately 837 MB. After recovery, both territorial counts were zero (transaction rollback); the two audit runs were explicitly closed as `FAILED`, with `row_count=0`. Database size remained approximately 427 MB, including approximately 328 MB allocated to the empty `address_accesses` relation. Do not retry the full import on the current capacity. Confirm the project's plan/limits and provide sufficient database/disk headroom on this same LAB project before retrying; plan any space reclamation separately, without blindly truncating or vacuuming operational tables. The runbook's post-import validation, Zone/MANUAL and authenticated UI steps remain pending.

The four source points quarantined by the current conservative envelope (`9.6≤lon≤12.5`, `42.2≤lat≤44.5`) are: `35060626` Camaiore, VIA AURELIA, `(11.866752, 42.035831)`; `32370051` Camaiore, VIA MICHELANGELO BUONARROTI, `(13.362948, 38.117687)`; `20925024` Camaiore, VIA VENTICINQUE APRILE, `(17.121966, 39.367714)`; `24301081` Gaiole in Chianti, LOCALITA' CASA AL PINO / MONTELUCO, `(12.7540343, 42.7227879)`. The fourth exceeds the current longitude ceiling of `12.5`; the earlier analysis reported three with an unspecified wider box, so its exact old threshold cannot be reproduced from the documented record. No source coordinate was corrected.

The live numeric PostGIS regression `EPSG:6706 → EPSG:3857` passed (`1225568.51638018, 5450227.06992851` against `1225568.516380178, 5450227.069928512`, tolerance 0.01 m); round-trip returned `11.0094693, 43.9015994` within 1e-7 degree. Real imported-point comparison remains pending because no points were committed.

## Lucca LAB execution — 2026-09-24/25

After confirming that the failed regional transaction left all territorial tables empty, targeted `VACUUM FULL` on only `address_accesses`, `street_official_revisions` and `streets` reclaimed rollback allocation (database approximately 427 MB → 24 MB). The Lucca subset from the unchanged 2026-09-15 regional ZIPs was then imported with `--province 046 --apply` and audit scope `PROVINCE:046`. Both runs are `APPLIED`: **10,426 Street**, **250,604 AddressAccess**, **33 municipalities**, 287 Locality, 1,464 zero-access Street, 215,891 source coordinates (215,888 valid points and three quarantined), 34,713 accesses without source coordinates. Database size after import was approximately **341 MB**. The same SHA pair/scope was rerun and returned a no-op. No CensusRecord was created by the import. Full Toscana remains unimported.

The three Lucca quarantines are `35060626`, `32370051` and `20925024` in Camaiore, with the source coordinates listed above. The fourth regional anomaly `24301081` belongs to Gaiole in Chianti (Siena) and is outside the Lucca subset. All three Lucca raw coordinates, methods and `EPSG:6706` provenance remain stored; none has an operative point. The structured numbering fields match their source revisions on all 250,604 accesses, with zero duplicate official IDs, zero orphan accesses, zero total-access mismatches and zero records outside Provincia `046`.

`tests/database/anncsu-lucca-live-verification.sql` passed against the persisted LAB. The approved PostGIS control passed; real access points from methods 1–5 matched the browser transform to within `5.6e-9` metres, below the 0.01 m tolerance. A LAB Zone in Bagni di Lucca associates two distinct `VIA CHIESA` official IDs (LUGLIANO and SAN CASSIANO DI CONTRONE): 137 official accesses, all available with zero CensusRecord. A motivated MANUAL Street and MANUAL AddressAccess were added to the same Zone with `PROPOSED` review, author/date/reason, tenant-operational scope and null ANNCSU IDs; the final Zone has three Streets and 138 accesses (137 located, one unlocated), all without CensusRecord. A second Zone association to one official Street was verified inside a rolled-back transaction. Anonymous read was denied, authenticated read allowed, direct Street update/audit read denied, and cross-Comune Zone association rejected.

Authenticated EXPLAIN ANALYZE on this Zone measured: Street search 1.383 ms, Locality filter 0.870 ms, Zone Streets 0.108 ms, Zone count RPC 5.709 ms, bounded 100-access page RPC 5.219 ms. The underlying Zone→Access join used `census_zone_streets_pkey` and `address_accesses_street_idx` (0.138 ms count, 0.172 ms page). These are LAB observations, not production service-level guarantees. The browser never receives the full 250,604-access catalog.

The exact Lucca import duration is unavailable: the original importer used transaction-start `now()` for `finished_at`, making audit timestamps misleading. Subsequent runs use `clock_timestamp()`. Lint, strict TypeScript, 216 Vitest tests and production build passed. Headless Playwright confirmed that an unauthenticated request to the Zone edit page redirects to `/login`; authenticated Playwright/visual UI verification still requires a Supabase Auth test-user password. Database credentials and the public client key do not create an Auth session. No authenticated UI pass is claimed until that check runs.
